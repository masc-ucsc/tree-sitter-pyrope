
mut b = 3
b  = 5        // OK
b += 1        // OK
cassert b == 6

const (a:u32,b2) = (1,"string_inferred")
cassert a == 1 and b2 == "string_inferred"

const d = "hello"  // OK

mut e:u32 = 33
cassert e == 33

